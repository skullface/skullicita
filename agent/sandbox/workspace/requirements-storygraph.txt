# Runtime deps for the vendored storygraph-wrapper (BrunoJurkovic/storygraph-wrapper).
# curl_cffi replaces upstream cloudscraper (blocked by current Cloudflare).
# html.parser is used instead of lxml to avoid native libxml2/libxslt builds.
curl_cffi>=0.16.0,<1
beautifulsoup4>=4.12.0,<5
pydantic>=2.6.0,<3
httpx>=0.27.0,<0.28
